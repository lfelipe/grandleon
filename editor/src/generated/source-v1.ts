/*
 * Generated from schemas/source/v1/project.schema.json.
 * Do not edit this file; run npm run generate:source-types.
 */

/**
 * Typed inert bindings retained for a future script ABI; imported code is never executed by authoring tools.
 *
 * @maxItems 64
 */
export type ScriptBindings = ScriptBinding[];
export type CampaignNode = {
  [k: string]: unknown;
} & {
  [k: string]: unknown;
} & {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  kind: "encounter" | "story" | "terminal";
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  mapId?: string;
  /**
   * Initial unit instances for an encounter. Order is not gameplay-significant.
   *
   * @maxItems 4096
   */
  placements?: EncounterPlacement[];
  objectiveIds?: string[];
  dialogueIds?: string[];
  /**
   * @maxItems 256
   */
  transitions: CampaignTransition[];
  /**
   * Members who join the company when this node completes, in the same batch as the node's other consequences. A recruit is defined where they join and is a stranger to every earlier board.
   *
   * @maxItems 64
   */
  recruits?: CampaignRosterMember[];
  /**
   * What passing this node puts in the company's store, in the same batch as the node's other consequences. Passing the node a second time grants again, because a grant is an event rather than a statement about how much the store should hold. Omitted and empty both mean the node gives nothing.
   *
   * @maxItems 64
   */
  grants?: CampaignItemGrant[];
  notes?: string;
  extensions?: Extensions;
  /**
   * Who acts next on this board, overriding the game's default. 'alternating' lets each side pick any unit in turn. 'sideBlocks' runs one whole side then the other and lets whoever holds a block act with any of its characters in any order. 'initiative' interleaves every unit by speed. Omitted means the project's 'defaultTurnOrder', which is alternating when the project states none.
   */
  turnOrder?: "alternating" | "sideBlocks" | "initiative";
  deployment?: DeploymentZone;
  /**
   * Scenes that play during this encounter, in the order they are authored. Only an encounter node may carry them: a story node has no board for anything to happen on.
   *
   * @maxItems 32
   */
  moments?: EncounterMoment[];
} & {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  kind: "encounter" | "story" | "terminal";
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  mapId?: string;
  /**
   * Initial unit instances for an encounter. Order is not gameplay-significant.
   *
   * @maxItems 4096
   */
  placements?: EncounterPlacement[];
  objectiveIds?: string[];
  dialogueIds?: string[];
  /**
   * @maxItems 256
   */
  transitions: CampaignTransition[];
  /**
   * Members who join the company when this node completes, in the same batch as the node's other consequences. A recruit is defined where they join and is a stranger to every earlier board.
   *
   * @maxItems 64
   */
  recruits?: CampaignRosterMember[];
  /**
   * What passing this node puts in the company's store, in the same batch as the node's other consequences. Passing the node a second time grants again, because a grant is an event rather than a statement about how much the store should hold. Omitted and empty both mean the node gives nothing.
   *
   * @maxItems 64
   */
  grants?: CampaignItemGrant[];
  notes?: string;
  extensions?: Extensions;
  /**
   * Who acts next on this board, overriding the game's default. 'alternating' lets each side pick any unit in turn. 'sideBlocks' runs one whole side then the other and lets whoever holds a block act with any of its characters in any order. 'initiative' interleaves every unit by speed. Omitted means the project's 'defaultTurnOrder', which is alternating when the project states none.
   */
  turnOrder?: "alternating" | "sideBlocks" | "initiative";
  deployment?: DeploymentZone;
  /**
   * Scenes that play during this encounter, in the order they are authored. Only an encounter node may carry them: a story node has no board for anything to happen on.
   *
   * @maxItems 32
   */
  moments?: EncounterMoment[];
};

export interface SourceProject {
  schemaVersion: "1.1.0";
  /**
   * Durable lowercase UUID identifying a package across names and revisions.
   */
  packageId: string;
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  gameId: string;
  title: string;
  contentRevision: string;
  /**
   * The season the ground of every map in this game is drawn in, on every board and in every ROM. Omit it and the game is drawn in the temperate theme, which is what it looked like before themes existed.
   */
  themeId?: "temperate" | "autumn" | "winter" | "ashland";
  /**
   * The style this game's characters are drawn in wherever a character names none of its own, on every board and in every ROM. It is a default and a theme, never a gate: a unit type may name any style on this menu, so a medieval game may field an undead enemy or a pirate captain drawn as such. A style supplies one drawing per archetype and nothing else: it never changes which archetype a class draws as, which colour a faction is drawn in, or any rule. Omit it and the characters that name no style of their own are drawn in the medieval style, which is what they looked like before styles existed.
   */
  characterStyleId?:
    "medieval" | "scifi" | "mythical" | "nature" | "sengoku" | "undead" | "pirates";
  /**
   * The body this game's characters are drawn with wherever a character names none of its own. 'first' is drawn male and 'second' female; the stored values are positions in a menu every client indexes by, and the words a person reads for them are the art library's. A figure is the body a role is drawn at, not the role: every figure draws all eight archetypes, in every style and every faction colour, and choosing one changes no stat, no reach and no rule. It is a default and never a gate, exactly as characterStyleId is. Omit it and the characters that name no figure of their own are drawn with the first, which is what they looked like before figures existed.
   */
  characterFigureId?: "first" | "second";
  /**
   * Who acts next in every battle in this game that does not say otherwise. 'alternating' lets each side pick any unit in turn. 'sideBlocks' runs one whole side then the other and lets whoever holds a block act with any of its characters in any order. 'initiative' interleaves every unit by speed. A battle that states its own turn order overrides this and is never rewritten by changing it. Omit it and battles that state nothing are alternating, which is what they were before this setting existed.
   */
  defaultTurnOrder?: "alternating" | "sideBlocks" | "initiative";
  /**
   * What becomes of a character in this game who falls in battle. 'permanent' is a death: the company never fields that character again, and their kit is what is left of them. 'recoverable' is a wound: they are carried off the field and rejoin the company once the battle is over, still holding whatever they were carrying when they went down. Under either, a character reduced to no health leaves the battlefield the moment they reach it, so this decides only what the campaign records after the battle and never what the battle itself does. Omit it and a character who falls is gone for good, which is what a fall meant before this setting existed.
   */
  characterLoss?: "permanent" | "recoverable";
  /**
   * Whether the members of the player's own company cannot be reduced below one health, so that a blow that would defeat one leaves them standing at one instead. This is a testing aid and not a way to play: it is here so that somebody walking their own campaign through can read the writing and follow the flow without having to deal with dying on the way. It is deliberately not a third value of characterLoss, because it is not an answer to the question that setting asks. It is compiled into the package like every other rule, and it changes what the battles do: the same project with it on and the same project with it off play differently, and two people holding the same package always get the same battles out of it. Omit it and nobody is protected from anything, which is what every character faced before this setting existed.
   */
  invulnerableForTesting?: boolean;
  /**
   * @maxItems 100000
   */
  classes: SourceClass[];
  /**
   * @maxItems 100000
   */
  weaponTypes?: SourceWeaponType[];
  /**
   * @maxItems 100000
   */
  itemTypes?: SourceItemType[];
  /**
   * @maxItems 100000
   */
  unitTypes: SourceUnitType[];
  /**
   * @maxItems 100000
   */
  weapons: SourceWeapon[];
  /**
   * @maxItems 100000
   */
  items: SourceItem[];
  /**
   * @maxItems 10000
   */
  maps: SourceMap[];
  /**
   * @maxItems 100000
   */
  factions?: SourceFaction[];
  /**
   * @maxItems 100000
   */
  abilities?: SourceAbility[];
  /**
   * @maxItems 100000
   */
  objectives?: SourceObjective[];
  /**
   * @maxItems 10000
   */
  campaigns?: SourceCampaign[];
  /**
   * @maxItems 100000
   */
  dialogues?: SourceDialogue[];
  notes?: string;
  extensions?: Extensions;
}
export interface SourceClass {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  notes?: string;
  extensions?: Extensions;
  baseStats: StatBlock;
  /**
   * Allowed weapon types. Omitted means legacy unrestricted; an empty array permits none.
   */
  allowedWeaponTypeIds?: string[];
  /**
   * Whether the unit keeps its remaining action points after attacking or using an ability. Omitted means striking ends the activation.
   */
  actsAfterAttacking?: boolean;
  /**
   * What this class can cross that open ground is not. Omitted means it walks: open ground only, which is what every class did before terrain could stop anyone.
   */
  traversal?: {
    /**
     * Whether the class flies. A flier crosses every terrain there is, including any added later, so it never needs listing them. Omitted means it does not fly.
     */
    flying?: boolean;
    /**
     * Terrain this class may enter on its own account, beside anything flight already grants. 'water' is river and sea; 'heights' is mountain and rock.
     *
     * @maxItems 2
     */
    crossings?: [] | ["water" | "heights"] | ["water" | "heights", "water" | "heights"];
  };
}
/**
 * Namespaced experimental data preserved by compatible tools.
 */
export interface Extensions {
  [k: string]: unknown;
}
export interface StatBlock {
  health: number;
  movement: number;
  /**
   * A number the damage arithmetic reads. The maximum is half of what the field holds, and it is an overflow bound rather than a design budget: a blow adds two of these together before subtracting a third, and the total has to fit the number health is kept in. Twice 16383 is one short of that number, so no pair can overflow it. A game wanting larger numbers than this wants a wider health field, which is a different change; the largest number in any shipped project is 55.
   */
  strength: number;
  /**
   * A number the damage arithmetic reads. The maximum is half of what the field holds, and it is an overflow bound rather than a design budget: a blow adds two of these together before subtracting a third, and the total has to fit the number health is kept in. Twice 16383 is one short of that number, so no pair can overflow it. A game wanting larger numbers than this wants a wider health field, which is a different change; the largest number in any shipped project is 55.
   */
  defense: number;
  /**
   * Subtracted from magical damage the way defense is subtracted from physical damage. Omitted means 0. Capped where every number the damage arithmetic reads is capped, so that no pair of them can overflow the number health is kept in.
   */
  resistance?: number;
  /**
   * Percentage points added to the chance a strike this character makes lands. The chance rolled is the weapon's or cast's accuracy, plus the striker's skill and luck, minus the target's evasion and luck, clamped to 0-100. Omitted means 0, which leaves the chance exactly at the authored accuracy.
   */
  skill?: number;
  /**
   * Percentage points on this character's side of every hit roll: added when they swing, subtracted when they are swung at. That symmetry is its whole role: there are no critical hits to avoid. Omitted means 0.
   */
  luck?: number;
  /**
   * Percentage points subtracted from the chance a strike against this character lands. Omitted means 0.
   */
  evasion?: number;
  /**
   * The magical counterpart to strength. A magical ability deals max(1, magic + power - resistance). A physical ability is not affected: strength already prices every basic attack, and adding it to an ability too would make the ability a swing under another name. Omitted means 0, which is the ability's power alone. Capped where strength is and for the same reason.
   */
  magic?: number;
  /**
   * Commands the unit may issue in one activation. Two lets it move and then attack, which is what the genre means by a turn. One is a character finished by its own first step: walking is a command and a command costs a point, so it may move or strike and never both. That is authorable on purpose, and a heavy thing that chooses between arriving and swinging is a real design. But it is a choice to make deliberately, because nothing on a board shows it and a player who walks into reach and is then refused a strike has met a rule they could not read. Omitted means one, as every omitted number in this block means the least this field admits.
   */
  actionPoints?: number;
  /**
   * Higher acts earlier when the encounter uses an ordered turn. Omitted means one.
   */
  speed?: number;
}
export interface SourceWeaponType {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  notes?: string;
  extensions?: Extensions;
}
export interface SourceItemType {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  notes?: string;
  extensions?: Extensions;
}
export interface SourceUnitType {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  classId: string;
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  factionId?: string;
  abilityIds?: string[];
  /**
   * Every weapon the character carries, in order. The first is the one in hand and defines the character's reach and power; the rest are choices an attack may name.
   */
  startingWeaponIds?: string[];
  startingItemIds?: string[];
  /**
   * The style this character alone is drawn in, overriding the game's. The menu is the project's menu: a game may field a character from any setting the library holds, so a medieval campaign may raise an undead enemy without becoming an undead game. A style supplies one drawing per archetype and nothing else: it never changes which archetype this character draws as, which colour its faction is drawn in, or any rule. Omit it and the character follows the game's characterStyleId, which is what every character did before this field existed.
   */
  characterStyleId?:
    "medieval" | "scifi" | "mythical" | "nature" | "sengoku" | "undead" | "pirates";
  /**
   * The body this character alone is drawn with, overriding the game's. 'first' is drawn male and 'second' female. Every figure draws every archetype in every style, so a figure is free to combine with any style: a mage drawn at the second figure is the same mage. Choosing one changes no stat, no reach and no rule. Omit it and the character follows the game's characterFigureId, which is what every character did before this field existed.
   */
  characterFigureId?: "first" | "second";
  growthRates?: GrowthRates;
  /**
   * The experience defeating one of these grants to whoever struck the felling blow. Omitted means 0, so a unit type nobody authored an award for is worth nothing and changes no campaign.
   */
  experienceAward?: number;
  /**
   * How much lifetime experience one of these needs for each level. Level is 1 + experience / experiencePerLevel, capped at 99. Omitted means 100.
   */
  experiencePerLevel?: number;
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  dropItemId?: string;
  /**
   * How often the drop falls, as a whole percentage rolled at the moment of defeat. The authored number is the rolled number. 100 always drops and rolls nothing. Authored together with dropItemId or not at all.
   */
  dropChance?: number;
  notes?: string;
  extensions?: Extensions;
}
/**
 * What a level-up may add. Each level gained rolls every stat named here exactly once, in the order health, strength, defense, resistance, movement, actionPoints, skill, luck, evasion, magic, and a success adds one point permanently. The last four are last because the order is the roll order and it is append-only: a growth seed already in use keeps producing the draws it produced. A unit type that authors nothing here never grows and never draws a number.
 */
export interface GrowthRates {
  /**
   * How often this stat gains a point on a level-up, as a whole percentage. The authored number is the rolled number: it is not averaged, weighted, or scaled by anything. Omitted means 0, which never rolls and never grows.
   */
  health?: number;
  /**
   * How often this stat gains a point on a level-up, as a whole percentage. The authored number is the rolled number: it is not averaged, weighted, or scaled by anything. Omitted means 0, which never rolls and never grows.
   */
  strength?: number;
  /**
   * How often this stat gains a point on a level-up, as a whole percentage. The authored number is the rolled number: it is not averaged, weighted, or scaled by anything. Omitted means 0, which never rolls and never grows.
   */
  defense?: number;
  /**
   * How often this stat gains a point on a level-up, as a whole percentage. The authored number is the rolled number: it is not averaged, weighted, or scaled by anything. Omitted means 0, which never rolls and never grows.
   */
  resistance?: number;
  /**
   * How often this stat gains a point on a level-up, as a whole percentage. The authored number is the rolled number: it is not averaged, weighted, or scaled by anything. Omitted means 0, which never rolls and never grows.
   */
  movement?: number;
  /**
   * How often this stat gains a point on a level-up, as a whole percentage. The authored number is the rolled number: it is not averaged, weighted, or scaled by anything. Omitted means 0, which never rolls and never grows.
   */
  actionPoints?: number;
  /**
   * How often this stat gains a point on a level-up, as a whole percentage. The authored number is the rolled number: it is not averaged, weighted, or scaled by anything. Omitted means 0, which never rolls and never grows.
   */
  skill?: number;
  /**
   * How often this stat gains a point on a level-up, as a whole percentage. The authored number is the rolled number: it is not averaged, weighted, or scaled by anything. Omitted means 0, which never rolls and never grows.
   */
  luck?: number;
  /**
   * How often this stat gains a point on a level-up, as a whole percentage. The authored number is the rolled number: it is not averaged, weighted, or scaled by anything. Omitted means 0, which never rolls and never grows.
   */
  evasion?: number;
  /**
   * How often this stat gains a point on a level-up, as a whole percentage. The authored number is the rolled number: it is not averaged, weighted, or scaled by anything. Omitted means 0, which never rolls and never grows.
   */
  magic?: number;
}
export interface SourceWeapon {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  weaponTypeId?: string;
  /**
   * A number the damage arithmetic reads. The maximum is half of what the field holds, and it is an overflow bound rather than a design budget: a blow adds two of these together before subtracting a third, and the total has to fit the number health is kept in. Twice 16383 is one short of that number, so no pair can overflow it. A game wanting larger numbers than this wants a wider health field, which is a different change; the largest number in any shipped project is 55.
   */
  power: number;
  /**
   * Legacy single reach. Prefer minimumRange and maximumRange; when only this is present it means a band of 1 to range.
   */
  range?: number;
  notes?: string;
  extensions?: Extensions;
  /**
   * Closest tile distance this weapon can strike. A bow with minimumRange 3 cannot hit an adjacent enemy.
   */
  minimumRange?: number;
  /**
   * Furthest tile distance this weapon can strike.
   */
  maximumRange?: number;
  /**
   * How often this weapon lands, as a whole percentage. Omitted means 100: it always lands and rolls nothing. Anything below rolls once per strike, and the number a player is shown is this number.
   */
  accuracy?: number;
}
export interface SourceItem {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  itemTypeId?: string;
  stackLimit: number;
  /**
   * What spending this item in battle does. Omitted means nothing does: the item may be carried, listed and counted, and a character asked to use it is refused. That is what every item authored before items could be spent says, so no existing content becomes usable by accident.
   */
  kind?: "restore";
  /**
   * How much a restoring item gives back, clamped to the health the character is missing. Required by kind restore and read by nothing else; the number a player is shown before spending is exactly this one, because using an item rolls nothing. The floor is one rather than the nought every other number in the damage arithmetic has, because an item that restores nothing is an item with no kind; the ceiling is theirs.
   */
  power?: number;
  notes?: string;
  extensions?: Extensions;
}
export interface SourceMap {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  width: number;
  height: number;
  /**
   * @maxItems 65536
   */
  terrain: string[];
  notes?: string;
  extensions?: Extensions;
}
export interface SourceFaction {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  /**
   * The colour this faction's characters wear on every board and in every ROM. Omit it and the faction takes the menu colour matching its position in the faction list.
   */
  color?: "blue" | "red" | "green" | "violet" | "amber" | "bone";
  notes?: string;
  extensions?: Extensions;
}
export interface SourceAbility {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  /**
   * What the area does to whoever it covers. Damage falls on the caster's opponents alone; restore mends whoever is standing in it.
   */
  kind: "damage" | "restore";
  /**
   * Physical damage is reduced by defense, magical by resistance. Ignored when kind is restore.
   */
  damageType?: "physical" | "magical";
  /**
   * Tiles affected around the targeted position.
   */
  areaShape?: "single" | "cross" | "diamond";
  /**
   * Used only by the diamond shape.
   */
  radius?: number;
  /**
   * A number the damage arithmetic reads. The maximum is half of what the field holds, and it is an overflow bound rather than a design budget: a blow adds two of these together before subtracting a third, and the total has to fit the number health is kept in. Twice 16383 is one short of that number, so no pair can overflow it. A game wanting larger numbers than this wants a wider health field, which is a different change; the largest number in any shipped project is 55.
   */
  power: number;
  minimumRange: number;
  maximumRange: number;
  /**
   * How often this cast lands, as a whole percentage. Omitted means 100: it always lands and rolls nothing. A damaging area rolls once per opposing unit it covers, and not at all for the caster's own side, which it cannot harm; a restoring ability never rolls.
   */
  accuracy?: number;
  scriptBindings?: ScriptBindings;
  notes?: string;
  extensions?: Extensions;
}
export interface ScriptBinding {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  slot: string;
  apiVersion: string;
  /**
   * Lexically validated project-relative path. The editor stores but never executes this script.
   */
  scriptPath: string;
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  entryPoint: string;
  /**
   * @maxItems 256
   */
  parameters: ScriptParameter[];
}
export interface ScriptParameter {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  name: string;
  value:
    | {
        kind: "boolean";
        value: boolean;
      }
    | {
        kind: "integer";
        value: number;
      }
    | {
        kind: "string";
        value: string;
      }
    | {
        kind: "contentRef";
        category:
          | "class"
          | "unit_type"
          | "weapon_type"
          | "weapon"
          | "item_type"
          | "item"
          | "map"
          | "faction"
          | "ability"
          | "objective"
          | "campaign"
          | "dialogue";
        /**
         * Stable, case-sensitive source identity. Renaming requires reference migration.
         */
        sourceKey: string;
      };
}
export interface SourceObjective {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  /**
   * How the objective is decided. Omitted means defeatAllOpponents. 'surviveRounds' is satisfied the moment the authored round completes, and failed when the objective's own side has nobody left in the battle.
   */
  kind?: "defeatAllOpponents" | "defeatTarget" | "protectTarget" | "surviveRounds";
  /**
   * The side the objective belongs to. Satisfying it favours that side.
   */
  side?: "first" | "second";
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  targetPlacementId?: string;
  /**
   * How many rounds a surviveRounds objective is about. A round is one pass through the turn order: everybody still standing having acted under sideBlocks and initiative, and one turn for each side under alternating. Required by surviveRounds and refused on every other kind, because a count nothing can read is a number an author meant something by.
   */
  rounds?: number;
  scriptBindings?: ScriptBindings;
  notes?: string;
  extensions?: Extensions;
}
export interface SourceCampaign {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  flow?: CampaignFlow;
  /**
   * The company this campaign begins with. Every member is a distinct persistent character even when two share a unit type, and a first-side placement fields one of them by naming it. A campaign played as a kept campaign needs at least one member; omitting the roster leaves a campaign that can only be walked without persistence.
   *
   * @maxItems 512
   */
  roster?: CampaignRosterMember[];
  /**
   * What the company's shared store holds the day it is founded, put there by the founding batch beside the members and their kits. This is the store rather than anybody's hands: a member's own satchel is their unit type's startingItemIds. Omitted and empty both mean a store that begins empty, which is what every campaign authored before this says, because a company that owns nothing and a company stated to own nothing are the same company.
   *
   * @maxItems 256
   */
  startingStore?: CampaignItemGrant[];
  scriptBindings?: ScriptBindings;
  /**
   * Objective membership set; array order does not define execution order.
   */
  objectiveIds?: string[];
  /**
   * Dialogue membership set; array order does not define execution order.
   */
  dialogueIds?: string[];
  notes?: string;
  extensions?: Extensions;
}
export interface CampaignFlow {
  contractVersion: "1.0.0";
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  entryNodeId: string;
  /**
   * @minItems 1
   * @maxItems 10000
   */
  nodes: [CampaignNode, ...CampaignNode[]];
}
export interface EncounterPlacement {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  memberId?: string;
  /**
   * What to call this character on this board: a boss, a captain worth talking to, anybody the player should be able to refer to by name. Omitted is a character named after their unit type, with an ordinal when more than one of that type stands on the board - so a name is never absent, only derived. A placement that names a memberId does not need one: the roster member's own name already answers this, and outranks it.
   */
  name?: string;
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  unitTypeId: string;
  side: "first" | "second";
  x: number;
  y: number;
  /**
   * How an unattended unit acts on its own turn. Omitted means hold.
   */
  behavior?: "hold" | "patrol" | "pursue";
  /**
   * Makes this character talkable on this board, and names the world flag that talking to them raises. Omitted means a character no talk may reach, which is what every placement authored before the talk gesture means. Authored here rather than on the unit type because being talkable is a fact about one character on one board.
   */
  talk?: {
    /**
     * Stable, case-sensitive source identity. Renaming requires reference migration.
     */
    flagId: string;
  };
  /**
   * Makes this placement a reinforcement: it is not on the board when the battle opens, and enters as its round begins. Omitted is a character standing on the board from the opening, which is what every placement authored before waves means. Everything else about the placement still applies - its stance above most of all, which is how a wave is authored to come at the player rather than stand where it spawned. Its tile is judged in bounds and passable exactly as any placement's is, but not against the tiles other placements hold: a character who comes in on the sixth round does not share the board with the opening arrangement, and where its tile is taken when its round comes it takes the nearest one it could stand on instead. A placement that names a memberId cannot author one.
   */
  arrival?: {
    /**
     * The round in progress this character enters on, one based. The earliest is the second, because the first round is the round the battle opens in and somebody who is there when it opens is an ordinary placement.
     */
    round: number;
    /**
     * How many rounds separate one arrival from the next. Authored together with 'times' or not at all: a gap with no number of arrivals is a stream no battle could outlast.
     */
    every?: number;
    /**
     * How many arrivals this placement makes in all. Authored together with 'every' or not at all: several arrivals with no gap between them would land on one round, which is a stack rather than a wave. The first arrival is this placement; every later one is a character of the same shape with an identifier the engine allocates.
     */
    times?: number;
  };
  /**
   * Visited in order by the patrol behaviour.
   *
   * @maxItems 32
   */
  patrolPoints?: {
    x: number;
    y: number;
  }[];
}
export interface CampaignTransition {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  targetNodeId: string;
  priority: number;
  when?:
    | (
        | {
            kind: "objectiveResult";
            /**
             * Stable, case-sensitive source identity. Renaming requires reference migration.
             */
            objectiveId: string;
            /**
             * Stable, case-sensitive source identity. Renaming requires reference migration.
             */
            result: string;
          }
        | {
            kind: "inventoryAtLeast";
            /**
             * Stable, case-sensitive source identity. Renaming requires reference migration.
             */
            itemId: string;
            quantity: number;
          }
        | {
            kind: "worldFlagEquals";
            /**
             * Stable, case-sensitive source identity. Renaming requires reference migration.
             */
            flagId: string;
            value: boolean | number | string;
          }
      )
    | {
        kind: "all";
        /**
         * @minItems 1
         * @maxItems 64
         */
        conditions: [
          (
            | {
                kind: "objectiveResult";
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                objectiveId: string;
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                result: string;
              }
            | {
                kind: "inventoryAtLeast";
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                itemId: string;
                quantity: number;
              }
            | {
                kind: "worldFlagEquals";
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                flagId: string;
                value: boolean | number | string;
              }
          ),
          ...(
            | {
                kind: "objectiveResult";
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                objectiveId: string;
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                result: string;
              }
            | {
                kind: "inventoryAtLeast";
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                itemId: string;
                quantity: number;
              }
            | {
                kind: "worldFlagEquals";
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                flagId: string;
                value: boolean | number | string;
              }
          )[]
        ];
      }
    | {
        kind: "any";
        /**
         * @minItems 1
         * @maxItems 64
         */
        conditions: [
          (
            | {
                kind: "objectiveResult";
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                objectiveId: string;
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                result: string;
              }
            | {
                kind: "inventoryAtLeast";
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                itemId: string;
                quantity: number;
              }
            | {
                kind: "worldFlagEquals";
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                flagId: string;
                value: boolean | number | string;
              }
          ),
          ...(
            | {
                kind: "objectiveResult";
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                objectiveId: string;
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                result: string;
              }
            | {
                kind: "inventoryAtLeast";
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                itemId: string;
                quantity: number;
              }
            | {
                kind: "worldFlagEquals";
                /**
                 * Stable, case-sensitive source identity. Renaming requires reference migration.
                 */
                flagId: string;
                value: boolean | number | string;
              }
          )[]
        ];
      }
    | {
        kind: "not";
        condition:
          | {
              kind: "objectiveResult";
              /**
               * Stable, case-sensitive source identity. Renaming requires reference migration.
               */
              objectiveId: string;
              /**
               * Stable, case-sensitive source identity. Renaming requires reference migration.
               */
              result: string;
            }
          | {
              kind: "inventoryAtLeast";
              /**
               * Stable, case-sensitive source identity. Renaming requires reference migration.
               */
              itemId: string;
              quantity: number;
            }
          | {
              kind: "worldFlagEquals";
              /**
               * Stable, case-sensitive source identity. Renaming requires reference migration.
               */
              flagId: string;
              value: boolean | number | string;
            };
      };
  notes?: string;
}
export interface CampaignRosterMember {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  unitTypeId: string;
  specificity?: CampaignMemberSpecificity;
  notes?: string;
}
/**
 * What makes this character more than their unit type: this archer shoots further, this knight is tougher. Applied where a character's earned level-up gains are applied, one step earlier, so an authored specificity and an earned gain are the same kind of thing to everything downstream and stack rather than replacing each other. Omitted is a character who is exactly their class, which is what every member authored before this says. Stating the object and then saying nothing in it is refused: it claims a character is specific without saying how. The whole shape serves the founding roster and every node's recruits alike, because a recruit is a member of the company from the moment they join.
 */
export interface CampaignMemberSpecificity {
  stats?: StatDeltaBlock;
  /**
   * Added to the maximum of the band of every weapon this character strikes with. On the character rather than on the weapon, because it is a fact about the archer and not about the bow: two archers carrying one authored bow have one bow between them, and only one of them was written to shoot further. It raises the ceiling and leaves the floor, so an archer written to outrange a swordsman still cannot answer one standing on top of her. Everything that reads a band follows it: striking, countering, the danger overlay, and the range a client's info sheet prints. An ability's reach is not affected: an ability's power and shape come from the ability rather than from the caster. Omitted is no bonus; zero is refused.
   */
  rangeBonus?: number;
}
/**
 * Signed adjustments over a unit type's stat line, one per stat, all optional. These are deltas and not totals, which is what lets an author rebalance the class underneath the character: the character keeps what was written about them and inherits everything that changed beneath it. They stack with what a character earns from levelling, because both are addends over the same authored line. Unlike growthRates this admits speed, because an authored delta is not a roll: it is fixed before the campaign is founded, identical on every playthrough and every platform, and read off the info sheet before the player commits to anything, so it reshuffles no turn order anybody was standing in.
 */
export interface StatDeltaBlock {
  /**
   * A signed number added to whatever the unit type says, at the moment the character takes the field. Omitted says nothing; zero is refused, because an author who wrote a number meant to change something. The sum must land inside the range this stat's own statBlock field admits: an author may make a character anything they could have made a class, and nothing they could not.
   */
  health?: number;
  /**
   * A signed number added to whatever the unit type says, at the moment the character takes the field. Omitted says nothing; zero is refused, because an author who wrote a number meant to change something. The sum must land inside the range this stat's own statBlock field admits: an author may make a character anything they could have made a class, and nothing they could not.
   */
  movement?: number;
  /**
   * A signed number added to whatever the unit type says, at the moment the character takes the field. Omitted says nothing; zero is refused, because an author who wrote a number meant to change something. The sum must land inside the range this stat's own statBlock field admits: an author may make a character anything they could have made a class, and nothing they could not.
   */
  strength?: number;
  /**
   * A signed number added to whatever the unit type says, at the moment the character takes the field. Omitted says nothing; zero is refused, because an author who wrote a number meant to change something. The sum must land inside the range this stat's own statBlock field admits: an author may make a character anything they could have made a class, and nothing they could not.
   */
  defense?: number;
  /**
   * A signed number added to whatever the unit type says, at the moment the character takes the field. Omitted says nothing; zero is refused, because an author who wrote a number meant to change something. The sum must land inside the range this stat's own statBlock field admits: an author may make a character anything they could have made a class, and nothing they could not.
   */
  resistance?: number;
  /**
   * A signed number added to whatever the unit type says, at the moment the character takes the field. Omitted says nothing; zero is refused, because an author who wrote a number meant to change something. The sum must land inside the range this stat's own statBlock field admits: an author may make a character anything they could have made a class, and nothing they could not.
   */
  skill?: number;
  /**
   * A signed number added to whatever the unit type says, at the moment the character takes the field. Omitted says nothing; zero is refused, because an author who wrote a number meant to change something. The sum must land inside the range this stat's own statBlock field admits: an author may make a character anything they could have made a class, and nothing they could not.
   */
  luck?: number;
  /**
   * A signed number added to whatever the unit type says, at the moment the character takes the field. Omitted says nothing; zero is refused, because an author who wrote a number meant to change something. The sum must land inside the range this stat's own statBlock field admits: an author may make a character anything they could have made a class, and nothing they could not.
   */
  evasion?: number;
  /**
   * A signed number added to whatever the unit type says, at the moment the character takes the field. Omitted says nothing; zero is refused, because an author who wrote a number meant to change something. The sum must land inside the range this stat's own statBlock field admits: an author may make a character anything they could have made a class, and nothing they could not.
   */
  magic?: number;
  /**
   * A signed number added to whatever the unit type says, at the moment the character takes the field. Omitted says nothing; zero is refused, because an author who wrote a number meant to change something. The sum must land inside the range this stat's own statBlock field admits: an author may make a character anything they could have made a class, and nothing they could not.
   */
  actionPoints?: number;
  /**
   * A signed number added to whatever the unit type says, at the moment the character takes the field. Omitted says nothing; zero is refused, because an author who wrote a number meant to change something. The sum must land inside the range this stat's own statBlock field admits: an author may make a character anything they could have made a class, and nothing they could not.
   */
  speed?: number;
}
/**
 * A quantity of one item, put into the company's shared store. The store is what the campaign holds beyond what its members carry, and it is the one owner an author can name without knowing who is still alive.
 */
export interface CampaignItemGrant {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  itemId: string;
  /**
   * How many. One is not the default and must be written, so that a list of things a company owns never has an omitted number in it.
   */
  quantity: number;
  notes?: string;
}
/**
 * What an encounter says about the player's own troops before the first activation: the region they are arranged in, how many of them may take the field, or both. Omitted means neither: no deployment phase, every character opens on the tile its placement names, and every member the board places who is fit to fight goes out. A stated deployment must say at least one of the two; one that states neither is refused by the compiler and both analyzers, where every other thing that is wrong with a deployment is also refused.
 */
export interface DeploymentZone {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  /**
   * The tiles of the region. Each must be on the encounter's map, must be ground a character can stand on, and must appear once; at least one first-side placement must stand on one of them, or the region is one nobody can be arranged in.
   *
   * @minItems 1
   * @maxItems 256
   */
  tiles?: [
    {
      x: number;
      y: number;
    },
    ...{
      x: number;
      y: number;
    }[]
  ];
  /**
   * How many of the company may take this field, at most. Omitted means no cap and is what every encounter says by default: every member the board has a placement for, and who is fit to fight, goes out. A cap is a maximum rather than a quota, so fewer is legal and the engine never benches anybody to make a party fit; a player who has fielded too many is refused by name and answers by benching somebody. Zero is not authorable: a board nobody may field onto is one with no first-side placements.
   */
  capacity?: number;
  notes?: string;
}
/**
 * A scene that plays during a battle, when something happens on the board. A node's own dialogueIds are presented on arrival, before the node acts, which is around a battle rather than inside one; these are inside it. Omitted means a battle nobody speaks during, which is what every encounter before this said.
 */
export interface EncounterMoment {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  /**
   * What has to happen for the scene to play.
   */
  when: {
    /**
     * stageOpens: once, as the board is first drawn. characterTalked: when the named character is talked off the board. characterFalls: when the named character is defeated, which is a different fact from being talked away and is reported by a different event.
     */
    kind: "stageOpens" | "characterTalked" | "characterFalls";
    /**
     * Which character on this board the moment is about. Required by characterTalked and characterFalls, and refused by stageOpens, which is about the board rather than about anybody. It names a placement's own id, the only identity stable across a battle.
     */
    placementId?: string;
  };
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  dialogueId: string;
}
export interface SourceDialogue {
  /**
   * Stable, case-sensitive source identity. Renaming requires reference migration.
   */
  id: string;
  name: string;
  /**
   * Ordered lines. Array order is presentation order.
   *
   * @maxItems 4096
   */
  lines?: {
    speaker: string;
    text: string;
  }[];
  /**
   * Who each speaker in this scene actually is, stated once for the scene rather than repeated on every line. Each entry names a speaker string exactly as the lines spell it and the unit type whose character that speaker is, and a client that draws a face draws the same character the board draws for that unit type: the same style, the same figure, the same archetype and the same faction colour. It says nothing about the words: the speaker string stays exactly what it is, and a speaker called 'Evil Dragon' still reads 'Evil Dragon'. Presentation only: no rule reads it and it never enters the canonical state hash. A speaker no entry names, and a scene with no cast at all, is drawn the way each client drew a speaker before a scene could name one.
   *
   * @maxItems 255
   */
  cast?: {
    /**
     * The speaker string this entry is about, matched against a line's own speaker exactly and case-sensitively. Spell it the way the lines spell it.
     */
    speaker: string;
    /**
     * Stable, case-sensitive source identity. Renaming requires reference migration.
     */
    unitTypeId: string;
  }[];
  /**
   * What this scene is drawn against, chosen from the backdrops the art library provides. A backdrop is a run of flat horizontal bands rather than a picture, so it costs no client a texture; a client with too few colours for the bands draws the one colour the backdrop names as its wash. It belongs to the scene rather than to the story node, so a cutscene that moves from the hall to the wall names two. Presentation only: no rule reads it and it never enters the canonical state hash. Omit it and the scene is drawn on the flat fill each client already used, which is what a scene looked like before backdrops existed.
   */
  backgroundId?:
    | "throne_hall"
    | "night_camp"
    | "deep_wood"
    | "mountain_dusk"
    | "open_sea"
    | "star_field"
    | "crypt";
  scriptBindings?: ScriptBindings;
  notes?: string;
  extensions?: Extensions;
}
